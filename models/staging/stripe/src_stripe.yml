version: 2

sources:
  - name: stripe
    database: RAW
    schema: STRIPE
    tables:
      - name: payment
        identifier: PAYMENT
        loaded_at_field: _batched_at
        freshness:
          warn_after:
            count: 12
            period: hour
          error_after: {count: 3, period: day}